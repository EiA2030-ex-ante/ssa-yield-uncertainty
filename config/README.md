# `config/` — pipeline configuration (placeholder)

This directory will contain the centralized configuration (`config.yml`) holding all
modelling constants: random seed, year ranges, fertilizer-rate grids, random-forest
hyperparameters, copula parameters, economic calibration knobs, and graphics settings.
Released with the working paper.
