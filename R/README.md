# `R/` — shared helpers (placeholder)

This directory will contain the shared utility functions used by every pipeline stage
(path resolution, configuration loading, SSA boundary and raster loaders, rainfall
feature extraction, plotting templates). Released with the working paper.
